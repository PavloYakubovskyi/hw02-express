const Contact = require("./contactModel");
const schemas = require("./contactModel");
const User = require("./userModel");
const schemas = require("./userModel");

module.exports = {
  Contact,
  User,
  schemas,
  schemas,
};
